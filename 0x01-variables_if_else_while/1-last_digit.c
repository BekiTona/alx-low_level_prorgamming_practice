#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - the code checks if the last digit of an intger has different values
 *
 * Return: always (0)
 **/
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
				
					return (0);
}
